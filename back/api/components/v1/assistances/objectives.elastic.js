const mappings = {
  dynamic: false,
  properties: {
    clusterObjective: {
      type: 'keyword'
    },
    sectoralObjective: {
      type: 'keyword'
    }
  }
}

module.exports = mappings
