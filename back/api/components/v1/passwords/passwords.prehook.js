const processBody = (body) => {
  return body
}

const processParams = (params) => {
  return params
}

const processQuery = (query) => {
  return query
}

module.exports = {
  processBody,
  processParams,
  processQuery
}
